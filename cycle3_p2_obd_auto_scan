Cycle3 P2: Expand OBD auto Bluetooth scan + discovery in ObdRpmProvider - DONE

- Limited inspection: used list_dir + multiple grep (broad + path-restricted to ObdRpmProvider/ANCService/AudioEngine/gradles/manifest/prefs without full re-reads of limited files). Performed exactly 1 read_file (full, relative path) on ObdRpmProvider before any edits; zero read_file on ANCService (only grep for usage check, which is via sessionContext.entitlement only).
- ANCService: no changes (minimal update not needed; direct usage of provider is in AudioEngine via obdRpmProvider?.start()/currentSnapshot()/stop(), snapshot API + RpmSnapshot unchanged).
- Implemented in existing ObdRpmProvider.kt only (relative path, search_replace exclusively; no new .kt sources):
  * Added imports for BroadcastReceiver, Intent, IntentFilter, Build (cleaned duplicate withTimeoutOrNull).
  * Added ObdState enum (IDLE/MANUAL/SCANNING/CONNECTING/CONNECTED/FALLBACK) + @Volatile obdState.
  * Rewrote start(): keep manual_test priority (exact), then manual_address path (existing) or new auto when blank; initial snapshot reflects "scanning"/"connecting"; launch background always for async auto.
  * Added phase: logs throughout (e.g. "phase: auto_scan_start", "phase: scanning_bonded", "phase: start_discovery_scan", "phase: found_elm_like", "phase: connecting", "phase: obd_connect_failed_fallback", "phase: connected_polling", "phase: stopped").
  * Refactored connect(addr) -> delegates to new connectToDevice(BluetoothDevice) [shared, with persist addr on success for future starts].
  * New isElmLike(name): matches ELM, OBD, OBDII, ELM327, VGATE, KONNWEI, SCANTOOL, CAR DOCTOR etc.
  * New autoDiscoverAndConnect(): bonded first (no scan), then adapter.startDiscovery() + temp BroadcastReceiver (ACTION_FOUND + FINISHED), compat register for SDK>=33 (RECEIVER_NOT_EXPORTED), 12s timeout + early exit, cancel always in finally; try connectTo on matches; suppress MissingPermission + deprecation for EXTRA.
  * Updated stop() to reset state + log phase; improved connect logs + error paths.
  * Kept RpmSnapshot API, currentSnapshot, poll loop, query/init/write/parse/close, SPP_UUID, manual path 100% compatible.
  * Fallback state when no device/addr/BT/connect fails (enables engine to fallback using manual RPM if configured).
- Used todo_write at start + updates for progress (cycle3_p2_* items).
- Relative compile check (windows powershell, relative gradlew as required):
  .\gradlew.bat :shared:compileDebugKotlinAndroid :shared:compileDebugKotlinMetadata :app:compileDebugKotlin --console=plain
  (full output in build-check-cycle3-p2.log next to this marker)
  BUILD SUCCESSFUL (no errors from new scan logic, states, receivers, version checks).
- Followed all instructions strictly: decisive/focused, relative paths only, search_replace/write, 1 limited read_file on target + greps, no ANCService reads/edits, no new kt, created only required non-md todo marker cycle3_p2_obd_auto_scan, phase logs + auto scan + state machine, end compile, TASK COMPLETE. No extra scope/broadening.

Status: TASK COMPLETE (see final writeup). No extra scope.