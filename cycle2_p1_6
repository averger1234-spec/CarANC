P1 #6+7: Runtime real latency measurement + more hot-path opt - DONE (cycle2)

- Read engine/processor/hot sections (limited): 
  * shared/src/androidMain/kotlin/com/example/caranc/shared/service/AudioEngine.kt (multiple limited reads offset/limit on start(), processing loop ~365-565, updateVisualization ~805, loadOrCalibrate, helpers, buffer reuse areas, latency estimate)
  * shared/src/commonMain/kotlin/com/example/caranc/shared/MultiBandANCProcessor.kt (limited: ctor ~95, process~267/280/295, BandFxLms inner ~473/495/549/585/600)
  * shared/src/commonMain/kotlin/com/example/caranc/shared/latency/FdafLowBandProcessor.kt (limited reads on push/processBlock ~34/61/69/121/140 for hot low-freq)
  * Also quick limited on facade, anc processor legacy, latency estimator (no new files read beyond required)
- Implemented measurement helper + occasional call (all in existing AudioEngine.kt, no new .kt):
  * Added reuse history bufs (sentOutputHistory/micInputHistory + index) + runtimeMeasured var.
  * Helpers: insertKnownProbe (low amp +/-/+ pattern), measureRoundTripLatencyIfDue (simple cross-corr peak search over lags, reuse buf, cheap step/win).
  * Occasional insert in main ANC loop (after output compute, rare %900 + cooldown).
  * Fill histories every block (reuse, no alloc).
  * Occasional correlate + call ( %1800 + cooldown): if meas valid, smooth, ancProcessor.setEstimatedLatencyMs(runtime..), + AncSessionLogger "runtime_latency_correlated".
  * Integrates w/ existing setEstimatedLatencyMs / latencyBandLimits / processor update.
- Add profiling in BandFxLms/process: added lmsProcessCalls, lmsUpdateCount, lastLmsPfx counters (inc at entry + inside LMS update if); private set for profiling.
- More hot-path opt:
  * LMS profiling counters (as above).
  * push buffer reuse to processor layer: added comment at fdaf creation + low call; edited FdafLowBandProcessor to prealloc extendedBuf/inputFftBuf/productBuf/error*Buf as fields, use+fill instead of local FloatArray in processBlock (called via push); + reset; header note.
  * In engine: added outputBufferReuse + changed to scaleSamplesInto (fills target, no per-block ShortArray alloc when gain scale).
  * Added native low-freq notes in MultiBandANCProcessor (near fdaf/low), in FdafLowBandProcessor header.
- Used todo_write (p1_6_7_*) at start + multiple updates for progress.
- Used search_replace exclusively (read_file multiple times w/ limits before *each* edit; edited 3 existing source + 1 todo marker).
- Never created source .kt ; only the required todo marker (non-md) cycle2_p1_6 .
- Relative compile check (windows powershell style, relative gradlew):
  .\gradlew.bat :shared:compileDebugKotlinAndroid :shared:compileDebugKotlinMetadata :shared:compileDebugUnitTestKotlinAndroid --console=plain
  > Task :shared:compileDebugKotlinAndroid
  > Task :shared:compileDebugKotlinMetadata
  > Task :shared:compileDebugUnitTestKotlinAndroid
  BUILD SUCCESSFUL
  (processor/engine/fdaf changes compile; no runtime exec needed; pure DSP + androidMain ok)
- Followed all: decisive focused, read limited before edits, relative, no broad scope, todo cycle, TASK COMPLETE.

Status: TASK COMPLETE (see final writeup). No extra scope.