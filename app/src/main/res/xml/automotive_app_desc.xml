<?xml version="1.0" encoding="utf-8"?>
<automotiveApp>
    <!-- Car App Library template host (phone projection / template HU). -->
    <!-- #11: This is NOT an OEM AAOS exclusive RNC. Full vehicle HAL + exclusive audio needs a separate automotive product. -->
    <uses name="template" />
</automotiveApp>
