# Single source of truth for Play / App versioning (read by app/build.gradle.kts).
# versionCode MUST increase for every Play upload (internal test or production).
# Also bump VERSION_NAME when shipping a meaningful feature set for road-test tracking.
# See CHANGELOG.md for what each version changed.
VERSION_CODE=14
VERSION_NAME=1.2.12
